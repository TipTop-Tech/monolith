# Backend/server only. Never put this in src/ or any React/Vite frontend code.
OPENAI_API_KEY=your_openai_api_key_here

# Optional: override the model used by server/openaiRoutineServer.mjs
# OPENAI_ROUTINE_MODEL=gpt-4o-mini

# Optional: override backend port
# AGENT_SERVER_PORT=8787
