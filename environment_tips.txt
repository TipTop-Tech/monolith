# Server/backend only. Never put real API keys in React/Vite frontend code.
# Use one or both providers depending on what you want to test.
OPENAI_API_KEY=your_openai_key_here
GEMINI_API_KEY=your_gemini_key_here

# Optional model overrides
OPENAI_ROUTINE_MODEL=gpt-4o-mini
GEMINI_ROUTINE_MODEL=gemini-2.5-flash
AGENT_SERVER_PORT=8787
